# Terraform state — never commit (can contain secrets in plaintext)
*.tfstate
*.tfstate.*
.terraform/
crash.log

# Variable files — hold real account IDs / ARNs / bucket names. Use *.example as template.
*.tfvars
!*.tfvars.example

# Lambda build artifact (generated by deploy.sh / manual zip)
lambda_deployment.zip
