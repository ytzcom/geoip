apiVersion: v1
kind: Secret
metadata:
  name: geoip-api-credentials
  namespace: geoip-system
  labels:
    app.kubernetes.io/name: geoip-updater
    app.kubernetes.io/component: credentials
type: Opaque
stringData:
  # Replace with your actual API key
  api-key: "your_api_key_here"
  # Replace with your actual API endpoint
  api-endpoint: "https://your-api.execute-api.region.amazonaws.com/v1/auth"
---
# Alternative: Use SealedSecrets for better security
# apiVersion: bitnami.com/v1alpha1
# kind: SealedSecret
# metadata:
#   name: geoip-api-credentials
#   namespace: geoip-system
# spec:
#   encryptedData:
#     api-key: "encrypted_api_key_here"
#     api-endpoint: "encrypted_endpoint_here"